#!/usr/bin/env sh
# https://tinyapps.org/blog/mac/201107230700_throttle_process_os_x.html
pids=$(ps ax | grep -E 'gnome|chromium' | grep -v -E 'grep' | awk '{print $1}' | tr '\n' ' ')
sudo renice +18 -p $pids
