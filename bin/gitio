#!/usr/bin/env sh
if [ -z "$*" ]; then
  echo "Usage: gitio <url> [<code>]"
  exit 1
fi

url=$1
code=$2
if [ -z "$code" ]; then
  curl -i http://git.io -F "url=$url" | grep Location
else
  curl -i http://git.io -F "url=$url" -F "code=$code" | grep Location
  echo
  echo "http://git.io/$code"
fi
